syntax = "proto3";

import "transaction_request.proto";
import "transaction_response.proto";

service IndraDB {
    rpc Transaction (stream TransactionRequest) returns (stream TransactionResponse);
}
